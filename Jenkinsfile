currentBuild.displayName="SREG-HAR-#"+currentBuild.number
pipeline
 agent { label "slave1" }
    stages{
        stage("Checkeout-Scm"){
            steps{
                
            }
        }
    }
